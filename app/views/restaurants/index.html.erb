<ul>
	<% @restaurants.each do |restaurant| %>
	<li><%= link_to restaurant.name, restaurant_path(restaurant) %> | <%= restaurant.category.capitalize %> | <%= restaurant.reviews.count %> reviews </li>
	<% end %>
</ul>

<%= link_to "Add new restaurant", new_restaurant_path, :class => "btn btn-success btn-large mt-3"  %>